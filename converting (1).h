#ifndef _CONVERTING_H_   // preventing a  same header file form being included multiple time
#define _CONVERTING_H_
#include <stdio.h>    // standard input/output library  
#include <string.h>    // string manipulation library  
#include <stdlib.h>     // standard library function for memoery allocation, process control, converstion and other.
void converting(void); // function prototype for converting function
#endif  // end of inculde guard